import {expect} from 'chai';
import {{{Model}}Actions} from 'client/actions/{{model}}/{{model}}-actions';

describe('{{Model}}Actions', function() {
  it('should exists', function () {
  	expect({{Model}}Actions).to.exist;
  });
});