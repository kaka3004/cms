import {expect} from 'chai';
import {SessionActions} from 'client/actions/session/session-actions';

describe('SessionActions', function() {
  it('should exists', function () {
    expect(SessionActions).to.exist;
  });
});