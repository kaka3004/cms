import {expect} from 'chai';
import {CategoryActions} from 'client/actions/category/category-actions';

describe('CategoryActions', function() {
  it('should exists', function () {
    expect(CategoryActions).to.exist;
  });
});