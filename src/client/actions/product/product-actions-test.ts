import {expect} from 'chai';
import {ProductActions} from 'client/actions/product/product-actions';

describe('ProductActions', function() {
  it('should exists', function () {
    expect(ProductActions).to.exist;
  });
});