import {expect} from 'chai';
import {CartActions} from 'client/actions/cart/cart-actions';

describe('CartActions', function() {
  it('should exists', function () {
    expect(CartActions).to.exist;
  });
});